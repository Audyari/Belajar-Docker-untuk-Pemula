Mengambil Image dari Registry

================================

1. cek image yg teristall di pc :
    docker images

2. test install mogoDB
    * docker pull mongo 
    kalo mau menurut versinya seperti ini
    *docker pull mongo:4.1

=========================

1. "docker images" : Perintah ini akan menampilkan daftar Image yang tersedia di mesin lokal Anda, termasuk Image 'mongo'.
2. "docker ps" : Perintah docker ps adalah salah satu perintah dasar dalam Docker yang digunakan untuk melihat daftar kontainer Docker yang sedang berjalan.

===========================
