Container

=================

Docker Container adalah instance yang dijalankan dari sebuah Docker Image.

=====================

Dalam Docker, sebuah "instance" merujuk pada satu eksekusi atau realisasi nyata dari sebuah Docker Container. Jadi,
instance adalah contoh konkret dari sebuah Container yang sedang berjalan.


=======================

