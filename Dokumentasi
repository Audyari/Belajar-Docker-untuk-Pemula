Membuat Container

======================

1. docker container ls : atau docker container ls --all
    Perintah docker container ls adalah perintah dalam Docker yang digunakan untuk melihat daftar 
    container Docker yang sedang berjalan (aktif).


===============================

CARA BIKIN CONTAINER :

1. # Membuat container baru dengan nama "mongoserver1"
docker container create --name mongoserver1 mongo:4.1

# Menjalankan container "mongoserver1"
docker container start mongoserver1

# Melihat daftar container yang sedang berjalan
docker container ls


=================================
